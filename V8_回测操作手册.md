# 夔牛 V8 Adaptive 回测操作手册

> 目标：在 MT5 策略测试器中跑过去一年数据，3 分钟内启动，30 分钟内拿到第一份报告。

---

## 一、前置准备（一次性，约 2 分钟）

### 1. 复制 EA 到 MT5 数据目录

MT5 启动 → 顶部菜单 **文件 → 打开数据文件夹** → 进入 `MQL5\Experts\` 子目录。

把以下文件复制进去：
```
e:\Code\Easy-Deal\V8.mq5            →  MQL5\Experts\V8.mq5
e:\Code\Easy-Deal\V8_Default.set    →  MQL5\Profiles\Tester\V8_Default.set  (建议)
e:\Code\Easy-Deal\V8_Aggressive.set →  MQL5\Profiles\Tester\V8_Aggressive.set
```

> 也可以直接放 `Experts\` 目录下，加载预设时再浏览定位。

### 2. 编译 EA

打开 **MetaEditor**（MT5 里按 `F4` 或工具栏图标）：
- 左侧导航 → `Experts` → 双击 `V8.mq5` → 按 `F7` 编译。
- 看底部 **错误窗口**：应输出 `0 error(s), 0 warning(s)`。
- 若有错误：把错误信息粘贴给我，我立刻修。

### 3. 下载历史数据（关键！）

回到 MT5 → 顶部菜单 **工具 → 选项 → 图表** → 设置：
- `图表中的最大柱数` = `999999999`（无限制）
- `历史中的最大柱数` = `999999999`

然后 **工具 → 选项 → 服务器** → 确认已连接 broker。

打开你要回测的品种图表（建议 **XAUUSD** 或 **EURUSD**）→ 切到 `M1` 周期 → 按住 `Home` 键不放 5–10 秒，MT5 会自动下载历史数据。  
**重要**：测试器需要 **比测试周期更小的周期** 的数据来生成 tick，所以即使你测 M15，也要把 M1 数据下下来。

---

## 二、启动策略测试器（约 1 分钟）

### 1. 打开测试器

MT5 顶部菜单 **视图 → 策略测试器**（或 `Ctrl+R`）。底部会弹出 **策略测试器** 面板。

### 2. 配置「设置」标签页

| 字段 | 填写值 | 说明 |
|---|---|---|
| **Expert** | `V8.ex5`（你编译出的那份）| 必须 |
| **Symbol** | `XAUUSD` 或 `EURUSD` | 黄金或主流外汇 |
| **Period** | `M5` 或 `M15` | 推荐 M5（细粒度），M15 速度更快 |
| **Date** | `自定义周期` | |
| ↳ From | 一年前的今天 | 例如今天 `2026.05.22` 就填 `2025.05.22` |
| ↳ To | 今天 | `2026.05.22` |
| **Forward** | `否` | 第一次跑别启用 walk-forward |
| **Execution** | `Real ticks (基于实际跳价)` | **重要**：网格策略对精度极敏感 |
| **Modelling** | `每个跳价基于真实跳价` | 同上 |
| **Deposit** | `10000` | 起始 $1 万，便于直观 |
| **Leverage** | `1:100` 或与你实盘一致 | |
| **Optimization** | `禁用` | 第一次跑别开优化 |

### 3. 加载参数预设

测试器底部右下角 → **加载** 按钮 → 浏览到 `V8_Default.set` → 打开。  
你会看到参数自动填入「输入参数」标签页。

### 4. 启动

点击右下角绿色 **开始** 按钮。  
进度条显示「下载历史」「准备数据」「执行回测」三阶段。  
M5 跑 1 年大约 **3–15 分钟**（取决于 CPU）；M15 通常 1–3 分钟。

---

## 三、关注回测进行中的关键日志

切到测试器底部 **日志** 标签页，正常应看到：

```
✅ 夔牛 V8 Adaptive 启动 | Symbol=XAUUSD Period=M5 RegimeTF=M15 Magic=20260522
📊 风控: 单笔风险=1.00% | 回撤上限=15.00% | 日亏上限=5.00%
🔄 [状态切换] 未知 → 震荡 | ADX=18.3 ATR=0.00234 (avg 0.00210)
✅ 下单成功 ORDER_TYPE_BUY lots=0.05 sl=2643.50 tp=2655.20 deal=12345
🎯 [网格] 平仓: 篮子止盈 +15.42 USD
🔄 [状态切换] 震荡 → 趋势↑ | ADX=27.5 ...
✅ 下单成功 ORDER_TYPE_BUY lots=0.08 sl=2648.10 tp=0.00000 deal=12346
🛡 [BO] ticket=67890 保本 @ 2658.30 (profit=1.05 ATR)
📈 [BO] ticket=67890 追踪 SL→2660.50 (peak 2.15 ATR)
```

### 异常信号（出现立刻停手发我）

- ❌ `指标初始化失败 err=4301` → broker 没该品种或周期，换品种
- ❌ `下单失败 ... retcode=10018` (market closed) → 时间设置不对
- ❌ 大量 `下单失败 ... retcode=10014` (invalid volume) → broker 最小手数高于 0.01，需调整 `InpFixedLotFallback`
- ❌ `OrderCalcMargin failed` → 杠杆/保证金参数异常
- ⚠️ 完全没有"下单成功"日志，只有"拒绝下单" → 风控过严，把 `InpRiskPercent` 调到 0.5、`InpMaxSpreadPoints` 调到 100 再试

---

## 四、读懂回测报告

跑完后切到 **图形** + **结果** + **回报** 三个标签页。

### 重点观察指标（按优先级）

| 指标 | 健康范围 | 解读 |
|---|---|---|
| **净利润** | > 0 | 必须正 |
| **盈利系数 (Profit Factor)** | ≥ 1.3 | 总盈/总亏，越高越好 |
| **最大回撤 %** | ≤ `InpMaxDrawdownPct` (15%) | 超过说明熔断没起作用 |
| **夏普比率** | ≥ 1.0 | 风险调整后收益 |
| **总交易数** | 一年 100~1000 笔 | 太少=信号过严；太多=过度交易 |
| **胜率** | 网格策略 60–80% 正常 | 单看胜率没意义，要配合盈亏比 |
| **平均盈亏比** | ≥ 0.8 | 网格策略天生小赢大亏，需补足 |

### 净值曲线长什么样才合格

✅ **健康曲线**：缓慢上升，回撤段不超过 15%，无悬崖式暴跌  
❌ **不健康**：长期横盘 + 末尾一次性大跌（典型的网格爆仓）

---

## 五、给我反馈以便分析

跑完后请提供以下三样东西之一（截图/导出皆可）：

1. **回测报告 HTML**：右键测试器空白处 → `保存为报告` → 把 `.htm` 路径告诉我
2. **关键截图**：「图形」标签页的净值曲线 + 「回报」标签页的统计数字
3. **异常日志**：「日志」标签页的红色/橙色行（搜 `❌` `⚠`）

我会基于报告帮你做：
- 净值曲线异常段定位（哪段时间出问题？震荡误判？突破假信号？）
- 参数敏感性分析（哪些参数应优化、哪些应锁死）
- 给出**参数优化计划**（在策略测试器里开 Optimization 跑哪几个参数、什么范围）

---

## 六、不下单怎么办（v8.10+ 新增）

V8 现在内置 **诊断系统 + 控制面板**，可以直接告诉你为什么不下单。

### 操作步骤

1. 看图表**左上角看板**最后一行 `阻塞: xxx`，会显示最近一次拒单原因
2. 看图表**右上角按钮面板**，点击 `📋 输出诊断日志` 立即看完整诊断
3. 在「专家」或「日志」标签页看输出，会列出：
   - 当前市场状态、ADX/ATR 数值
   - 是否被风控/点差/熔断拦截
   - 突破单：Donchian 上下轨 vs 当前价的差距、ATR 是否加速
   - 网格单：ATR 间距、当前持仓数

### 典型诊断结论 → 对应解决方案

| 诊断显示 | 含义 | 调整方法 |
|---|---|---|
| `状态: 趋势↑` 但 `Ask>HH? 否 (差 -0.001)` | 价格还没突破 Donchian 上轨 | 等待 或 减小 `InpBO_Lookback` (如 10) |
| `ATR加速? 否 (cur=0.001 prev=0.002)` | ATR 在收窄，动量不足 | 降低 `InpBO_ATR_AccelMult` 到 `1.0` 或 `0.9` |
| `状态: 中性` | ADX 在缓冲带 20~25 | 调小 `InpADX_TrendMin` 到 `22` |
| `状态: 极端波动` | ATR 暴涨触发冻结 | 调大 `InpATR_HighVolMult` 到 `2.5` |
| `点差=120 (上限50)` | 实盘点差超限 | 调大 `InpMaxSpreadPoints` 到 `150` |
| `daily loss limit` | 已达单日亏损上限 | 等次日 或 调大 `InpDailyLossLimitPct` |

### 控制面板按钮说明

右上角 9 个按钮：

| 按钮 | 作用 |
|---|---|
| ⏸ 暂停 EA / ▶ 恢复 EA | 暂停只管存量, 不开新仓 |
| 🟢 网格 ON / OFF | 单独关闭网格引擎 |
| 🟢 突破 ON / OFF | 单独关闭突破引擎 |
| ↑ 手动买入测试 | 用 ATR×倍数 SL 立即开多 (测试通道) |
| ↓ 手动卖出测试 | 用 ATR×倍数 SL 立即开空 (测试通道) |
| ✕ 平网格仓 | 一键平掉所有网格持仓 |
| ✕ 平突破仓 | 一键平掉所有突破持仓 |
| ✕✕ 平全部 | 平掉本 EA 全部持仓 |
| 📋 输出诊断日志 | 立即输出完整诊断到专家日志 |

> **重要**：策略测试器中按钮也可点（点测试器底部图表），但优化模式（Optimization）下按钮不工作。

---

## 七、常见陷阱

| 陷阱 | 表现 | 解决 |
|---|---|---|
| 历史数据缺失 | 测试器秒结束，没交易记录 | 重新下载 M1 数据 |
| 点差过滤拒单 | 日志全是 `spread > limit` | 把 `InpMaxSpreadPoints` 调到 100 或 200 |
| 状态从未切换 | 日志只有"震荡"或只有"趋势" | 调 `InpADX_RangeMax` / `InpADX_TrendMin` |
| 突破单从未触发 | 日志无 `_B` 开头的下单 | 把 `InpBO_ATR_AccelMult` 降到 1.05 |
| 黄金品种没数据 | broker 命名可能是 `GOLD`/`XAUUSDm`/`XAUUSD.` | 在市场报价右键找到正确符号 |

---

**准备好开跑了吗？跑完把报告发我，我们进入参数优化阶段。**
