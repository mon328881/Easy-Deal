# 夔牛 EA V8 → V9 版本演进说明

> 一份**诚实**的开发复盘:这个 EA 在迭代过程中踩了哪些坑、每次"修复"产生了什么新问题、最后留下哪些待解决议题。
>
> 适合自己日后回看,也适合给协作者快速理解项目当前的真实状态。
>
> **技术规格 (时间周期 / 市场状态 / 策略路由)**: 见 [宙斯_开发文档.md](宙斯_开发文档.md) (宙斯 EA V10)  
> 夔牛 V9 归档: [夔牛_开发文档.md](夔牛_开发文档.md)

---

## 一、整体路线图

```
原始想法 (用户)
  │
  │  "ATR + ADX 判断市场状态, 震荡走网格 + 趋势走突破"
  ↓
V8.0  双引擎自适应  ────────────────┐
  ↓                                 │
V8.1  网格安全化重构  ←──── 灾难 1: 全年 -97% 爆仓
  ↓                                 │
V8.2  Trend-Only + 鞭打过滤器        │
  ↓                                 │
V8.2.1 出场逻辑修复  ←──── 问题 2: 全年仅 12 笔, R:R 0.70
  ↓                                 │
V8.3  H1 双周期硬过滤  ←──── 灾难 3: 全年 0 笔交易
  ↓                                 │
V9.0  新闻日历 + 极简化 ←──── 问题 4: 状态卡"未知"死锁
  ↓                                 │
V9.1  激进锁利 + 二元状态 ←──── 当前: 等待全年回测验证
                                    │
                                    ↓
                                 待办事项
```

---

## 二、各版本核心问题与"修复"

### V8.0 - 双引擎自适应 (起点)

**设计意图**:
- M15 ATR + ADX 识别市场状态
- ADX < 22 → 震荡 → 网格策略
- ADX ≥ 22 → 趋势 → 突破策略

**最初遇到的问题**:
| 问题 | 根因 | 解法 |
|---|---|---|
| 启动后一直不下单 | 默认 `InpMaxSpreadPoints=50`,XAUUSD 点差 300+ | 改默认值到 500 |
| 状态识别为"中性"不开仓 | ADX 在 22-23 缓冲区反复 | (后续 V8.1 调整阈值) |
| 网格 `deal_ticket ≠ position_ticket` | MQL5 对冲账户区分 deal 和 position | `SyncBOStates()` 改用 position 扫描 |

---

### V8.1 - 网格安全化 (第一次灾难)

**灾难日志**:
```
final balance:  267.17 USD  (从 10,000 USD 跌到 267)
回撤率:        -97%
原因:          网格在反复打耳光行情中两头挨打
```

**根因分析**:
- 网格策略的**死亡螺旋**: 假突破触发加仓 → 反向运动 → 再加仓 → 继续反向 → 持仓越加越多 → 浮亏放大 → 爆仓
- 单笔风险 1% 在网格场景下被规避了——网格的"风险"不是单笔, 而是**整篮浮亏**

**V8.1 的修复 (有用但不够)**:
- ✅ 新增「整篮浮亏 % 全平」机制
- ✅ 状态切换到趋势时强平所有网格仓
- ✅ 默认关闭网格 `InpEnableGrid = false`
- ✅ 收紧最大回撤 10% → 8%

**留下的隐患**:
- 即便关掉了网格,**突破单的胜率本身就是 20-30%**, 而非高胜率策略

---

### V8.2 - Trend-Only 重构 (第二次受挫)

**改动**:
- 默认关掉网格,只保留突破
- 加入**鞭打市场过滤器**: 60 分钟状态切换 > 3 次 → 冻结 4 小时
- 加入**趋势冷启动**: 状态切换后 2 根 K 线内不开仓
- 加入**单趋势期最多入场 N 笔** (默认 2)

**回测结果**:
```
总笔数:    12 笔  (全年)
胜率:      50%
盈亏比:    0.70    ← 关键问题
结果:      微亏 -0.46%
```

**残酷诊断**:
- 50% 胜率本身可以接受
- 但 R:R 0.70 意味着**赢的小、输的大**
- 出场参数不合理:
  - 保本触发 1.0 ATR → 太早,赢家被砍掉
  - 追踪启动 1.8 ATR、距离 1.5 ATR → 跟得太松,利润吐回
  - 止损 2.0 ATR → 单笔最大亏损 -14.80 USD

---

### V8.2.1 - 出场逻辑修复 (单点改进)

**仅调出场参数, 入场不动**:
| 参数 | V8.2 | V8.2.1 | 意图 |
|---|---|---|---|
| `InpBO_SL_ATR_Mult` | 2.0 | 1.5 | 单笔亏损减 25% |
| `InpBO_BE_TriggerATR` | 1.0 | 1.5 | 不过早保本 |
| `InpBO_TrailStartATR` | 1.8 | 2.5 | 让趋势真正发展 |
| `InpBO_TrailDistATR` | 1.5 | 0.8 | 锁利后抓得更紧 |

**问题**: V8.2.1 还没回测就被 V8.3 覆盖了。

---

### V8.3 - H1 双周期过滤 (第二次灾难: 0 笔交易)

**设计意图** (基于用户提供的黄金走势图分析):
- M15 信号容易被 H1 大趋势"假反弹"诱导
- 加入 H1 EMA50 vs EMA200 + H1 ADX 作为**硬过滤**
- M15 入场必须与 H1 大方向同向

**灾难日志**:
```
final balance:  350.00 USD (初始 4177)  
交易笔数:       0          ← 全年一单未下
拒单原因:       鞭打市场冻结中
                H1 偏好不一致 (M15 趋势↓ 但 H1 多头)
```

**根因分析 (现在回头看)**:
- 多层过滤器**叠加效应**: 鞭打过滤 × H1 硬过滤 × 冷启动 × 趋势期入场限制 = **不可能入场**
- H1 偏好硬过滤的**思路错了**:
  - 黄金 H1 多头时, M15 的强空头**正是高质量回调机会**
  - 硬过滤砍掉了这种"逆 H1 而行的优质交易"
- 鞭打过滤一旦触发就冻结 4 小时, 几乎覆盖整个交易日

---

### V9.0 - 新闻日历 + 极简化 (大刀阔斧重构)

**核心改动**:
| 模块 | V8.3 | V9.0 | 设计思路 |
|---|---|---|---|
| **新闻日历过滤** | ❌ 无 | ✅ MT5 `CalendarValueHistory` API | 真正的风险时段(NFP/CPI) ±60 分钟暂停 |
| **鞭打过滤器** | 60分3次→冻结4小时 | ❌ **删除** | 改用新闻过滤精准拦截 |
| **冷启动** | 2 根 K 线 | **0 根** | 删除多余等待 |
| **每趋势期入场限制** | 2 笔 | **10 笔** | 大幅放宽 |
| **H1 偏好** | 硬过滤 | **软参考** | 仅打印,不阻止 |
| **趋势跟随入场** | 单模式(回调) | 双模式(回调+追单) | 强趋势时追上 |

**新发现的问题**:
```
日志显示: 【状态】未知 | ADX=16.2~25.6 反复跳
        【最近拒单】当前状态[未知]不开新仓
```

**根因**: ADX_RangeMax=20, ADX_TrendMin=25 形成 20-25 的**中性带**, ADX 在缓冲区反复跨越阈值, 防抖期(3根K线)永远凑不齐, 状态机**永远停在初始 UNKNOWN**。

---

### V9.1 - 状态机二元化 + 激进锁利 (当前版本)

**修复清单**:
| 修复 | 旧值 | V9.1 | 说明 |
|---|---|---|---|
| ADX 中性带 | RangeMax=20, TrendMin=25 | **单一阈值 22** | 二元化:震荡或趋势,无中间 |
| 初始状态 | `REGIME_UNKNOWN` | `REGIME_RANGE` | 启动后立即可工作 |
| 防抖根数 | 3 根 | **2 根** | 加速确认 |
| 未知状态行为 | 拒绝开仓 | 走网格引擎 | 防御性 |
| 保本触发 | 1.5 ATR (~5 美元) | **0.8 ATR** (~2.5 美元) | 黄金高点差下提早锁利 |
| 追踪启动 | 2.5 ATR (~8 美元) | **1.5 ATR** (~5 美元) | 让 44 美元这种浮盈能落袋 |
| 追踪距离 | 0.8 ATR | **0.5 ATR** | 紧贴市价 |
| 追踪步长 | 0.3 ATR | **0.2 ATR** | 推进更敏感 |

**V9.1 实测的一笔交易**:
```
品种:   XAUUSDm
方向:   BUY 0.23 手
开仓:   4504.258
当前:   4505.194 (Bid)
浮盈:   +44.53 USD
ATR:    ~3.6 美元

旧规则: 还差很远才到保本触发 1.5 ATR
新规则: 浮盈已超 0.8 ATR, 应立即保本; 接近 1.5 ATR 启动追踪
```

---

## 三、所有版本的共同教训

### 教训 1: "加过滤器" ≠ "更安全"

V8.2/V8.3 一路在加: 鞭打过滤、冷启动、入场配额、H1 硬过滤……
- 每加一个,觉得"更安全了"
- 实际是**每加一个都砍掉了一部分入场机会**
- 叠加到 V8.3 时, 全年 0 笔交易, 等于策略不存在

**正确的思路**: 一个清晰的入场信号 + 严格的出场管理 > 多层入场过滤器。

### 教训 2: 黄金 (XAUUSD) 是个特殊品种

| 特性 | 影响 |
|---|---|
| 点差 300+ (Exness Mini) | 每开仓直接亏 3 美元/0.01 手 |
| ATR 3-5 美元 (M15) | 价格瞬间跑 10-20 美元远离均线 |
| 突破真实率 20-25% | 假信号是真信号的 3-4 倍 |
| 真趋势中多波段结构 | "趋势跟随"在每次回调时止损 |

**结论**: 单一策略很难在黄金上稳赚。需要 ATR/状态/时段/新闻 多维度配合。

### 教训 3: 状态机要二元化, 不要"中性带"

V8.2: RangeMax=20, TrendMin=25 (留 5 分缓冲带) → ADX 在缓冲带反复跳 → 状态机永远不切换。

V9.1: **单一阈值 22**, 上面=趋势 下面=震荡, 没有中间地带。

### 教训 4: 必须区分 "deal ticket" 和 "position ticket"

MT5 对冲账户中:
- `trade.ResultDeal()` 返回 **deal ticket** (交易事件 ID)
- `PositionGetInteger(POSITION_TICKET)` 返回 **position ticket** (仓位 ID)
- 两者**不相等**

最早 V8.0 用 deal_ticket 注册 BOState, 导致 `PositionSelectByTicket()` 找不到, 追踪止损完全失效。

修复: 不主动注册, 改用 `SyncBOStates()` 扫描 `PositionsTotal()` 自动注册。

### 教训 5: 出场比入场重要

V8.2 全年 12 笔, 胜率 50%, 但 R:R 0.70 还是亏钱。
V8.2.1 仅调出场参数 (没改入场), 理论上就能从微亏转为微盈。

**实战中**: 50% 胜率 + R:R 1.5 > 60% 胜率 + R:R 0.8。

---

## 四、当前版本 (V9.1) 的待办事项

### 短期 (回测验证类)

| 待办 | 优先级 | 说明 |
|---|---|---|
| 用 V9.1 全年回测 | 🔴 高 | 必须确认全年笔数 > 30 笔, 否则策略形同虚设 |
| 对比 V8.3 vs V9.1 同区间数据 | 🟡 中 | 验证 V9.1 真的解决了"0 笔"问题 |
| 检查新闻过滤命中频率 | 🟡 中 | 看日志, NFP/CPI 周确实暂停了吗 |
| 验证黄金 H1 偏好软参考效果 | 🟢 低 | 仅日志, 不影响交易 |

### 中期 (策略调优类)

| 待办 | 优先级 | 说明 |
|---|---|---|
| 调研黄金点差与经纪商选择 | 🔴 高 | Exness Mini 308 点 vs IC Markets Raw 15 点, 利润相差 20 倍 |
| 决策"网格引擎"最终命运 | 🟡 中 | 默认关闭, 是否在震荡期重启? 风险与收益怎么平衡? |
| 引入时段择时 (伦敦/纽约) | 🟢 低 | 已有 input 参数, 但默认未启用 |
| 探索 RSI 极值反转作为震荡期入场 | 🟢 低 | 替代网格的另一种思路 |

### 长期 (架构与扩展类)

| 待办 | 优先级 | 说明 |
|---|---|---|
| 多品种支持验证 | 🟢 低 | 当前参数都是黄金调的, EURUSD 等需重新校准 |
| 实盘小资金真实测试 | 🔴 高 | 回测再漂亮也要实盘验证 (滑点、订单拒绝、网络延迟) |
| 出场策略 A/B 测试 | 🟢 低 | V8.2.1 vs V9.1 的出场参数对比 |
| 加入资金管理曲线监控 | 🟢 低 | 实时监测策略漂移 |

---

## 五、关键决策记录 (Decisions Log)

| 时间 | 决策 | 当时的理由 | 现在的评价 |
|---|---|---|---|
| V8.0 | ATR+ADX 状态机 | 经典双引擎设计 | 方向正确, 但黄金这种品种状态变化太快 |
| V8.1 | 关闭网格 + 整篮止损 | 网格导致 -97% | 必要的安全措施, 但治标不治本 |
| V8.2 | 加鞭打过滤器 | 防止反复打耳光 | **过度设计**, 后被 V9 删除 |
| V8.3 | H1 硬过滤 | 防止大小周期对抗 | **方向对但实现错**, 应该用软参考 |
| V9.0 | 引入新闻日历 (借鉴 GMarket) | 真正的风险时段是新闻 | 设计合理, 待回测验证 |
| V9.1 | 状态二元化 + 激进锁利 | 黄金高点差 + 状态卡死 | 当前最合理的版本, 但还需验证 |

---

## 六、参考的其他 EA

| EA | 学到了什么 | 没用什么 |
|---|---|---|
| `复刻牛.mq5` | 多引擎架构、沙盒模式 | 主体仍是网格, 复杂但本质不变 |
| `天天向上-动态3.1.mq4` | 单方向马丁加仓 + 整体止盈 | 没有状态判断, 单边趋势必爆 |
| `GMarket.mq5` | **MT5 经济日历 `CalendarValueHistory` API** | 同样是马丁逻辑, 但加了新闻/ATR 护甲 |

---

> **诚实备注**: 这个 EA 经过 7 次迭代仍未在黄金上证明长期稳健盈利。这是个**常态**, 不是失败——市场上 95% 的零售 EA 都做不到。
>
> 接下来最关键的事是: 让 V9.1 跑出**至少有 30 笔交易**的全年回测, 拿到真实数据再讨论下一步。否则一切都是空谈。
