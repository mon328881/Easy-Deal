# 宙斯 EA 开发文档 (Zeus EA)

> **版本**: V10 (设计规格 — 基于 V8/V9 实战血泪迭代)  
> **代码基线**: 自 `V9.mq5` (夔牛 V9.1) 演进 → 目标 `Zeus_V10.mq5`  
> **关联**: [V8_V9_版本演进说明.md](V8_V9_版本演进说明.md) · [夔牛_开发文档.md](夔牛_开发文档.md) (历史归档)  
> **最后更新**: 2026-05-22

---

## 0. 设计总纲

### 0.1 一句话

**宙斯 V10 = 多周期感知 + 多状态量化 + 策略路由表**, 在**黄金高点差**约束下自动切换网格 / 突破 / 观望等子策略。

```
时间周期 (L0~L4)  ──→  指标计算 (ADX/ATR/结构)
                              │
                              ▼
市场状态 (12 类)  ──→  状态机 + 防抖
                              │
                              ▼
策略路由表        ──→  网格 | 突破 | 跟随 | 冻结 | 仅平仓
                              │
                              ▼
点差护甲层        ──→  所有下单前必须通过 (黄金专用)
```

### 0.2 与夔牛 V9 的关系

| 项目 | 夔牛 V9.1 | 宙斯 V10 |
|------|-----------|----------|
| 品牌 | 夔牛 (KN) | 宙斯 (Zeus) |
| 状态机 | 5 类 (震荡/趋势↑↓/高波动) | **12+ 类细分** |
| 策略切换 | 2 引擎 (网格/突破) | **路由表 + 多子策略参数集** |
| 点差 | 参数 `MaxSpreadPoints` | **点差护甲贯穿入场/出场/网格目标** |
| H1 过滤 | 软参考 | L1 多周期共振 (可硬/软) |
| 代码文件 | `V9.mq5` | `Zeus_V10.mq5` (待建) |

---

## 0.5 实战教训驱动的设计原则 (来自 V8/V9 的血泪)

> 这一章是宙斯 V10 与之前 7 个版本最大的区别: **每条设计原则都对应一次真实灾难/挫折**, 不是凭空臆想。

### 0.5.1 7 次迭代的教训提取

| # | 时期 | 真实事件 | 提取的原则 (V10 强制) |
|---|------|----------|------------------------|
| 1 | V8.0 启动 | 默认 `MaxSpread=50`, 黄金 308 点直接拒绝所有信号 | **品种感知默认值**: 启动时按 `_Symbol` 自动选 set |
| 2 | V8.0 网格 | `deal_ticket ≠ position_ticket` 导致 BOState 全部丢失 | **持仓追踪只用 position ticket**, 禁用 `trade.ResultDeal()` 注册 |
| 3 | V8.1 网格爆仓 | 反复打耳光行情 → -97% (350 → 267 USD) | **网格在黄金默认关闭**, 仅在 `spread_ATR<0.25` + 真实窄幅震荡才开 |
| 4 | V8.2 过滤器堆叠 | 鞭打+冷启动+H1硬过滤 → 全年 12 笔 | **过滤器总数硬上限 ≤ 3**, 新增需删除旧的 |
| 5 | V8.2.1 出场 | R:R 0.70 (赢家被早杀, 输家扛足止损) | **出场不对称**: BE 必须先于一半止损距离触发 |
| 6 | V8.3 H1 硬过滤 | 全年 **0 笔** 交易 | **多周期过滤默认软参考**, 硬过滤必须显式 `InpMacroMode=HARD` |
| 7 | V9.0 中性带 | ADX 20-25 缓冲带 → 状态卡"未知"死锁 | **状态二元化**, 禁止"中性"枚举参与开仓判断 |
| 8 | V9.0 初始态 | `g_regime = UNKNOWN` 开盘 N 根 K 线不交易 | **初始态默认 RANGE**, 而非 UNKNOWN |
| 9 | V9.1 趋势跟随 | 价格距 EMA 15 USD, 回调窗口 2 USD → 永远不触发 | **追单上限 ≥ 3 ATR**, 不是只看回调 |
| 10 | V9.1 浮盈 44 USD 不锁 | 用 1.5 ATR 衡量是 0.6 ATR, 远未到保本 | **保本触发**用 `max(0.8ATR, 2×spread_ATR)`, 黄金高点差时下沉 |

### 0.5.2 V10 的 10 条硬约束 (Hard Constraints)

任何代码改动**必须**通过以下检查:

| 约束 ID | 内容 | 检验方法 |
|---------|------|----------|
| **HC-01** | 黄金 (`XAU*`) 默认 `MaxSpreadPoints ≥ 400` | 代码搜 `XAU` / OnInit 自动 set |
| **HC-02** | `MaxSpread` 与 `spread_ATR` 双重护甲, 缺一不可 | `PassesSpreadGuard()` 单元 |
| **HC-03** | 任何 `IsXxxFrozen()` 函数总冻结时长 ≤ 4 小时 | 代码审计 + 日志统计 |
| **HC-04** | 入场过滤器 (除点差/熔断/新闻) 总数 ≤ 3 | 静态检查 |
| **HC-05** | 状态机不得有「中性带」, 阈值必须二元 | `ClassifyRegime()` 单元 |
| **HC-06** | 多周期过滤默认软参考 | 配置默认值 `InpMacroMode=SOFT` |
| **HC-07** | 出场 BE 触发 ATR 倍数 < 单笔 SL ATR 倍数 × 0.6 | 例如 SL=1.5 → BE≤0.9 |
| **HC-08** | 网格在高点差品种默认关闭 (`spread_ATR>0.25`) | OnInit 检查 |
| **HC-09** | 任何状态切换日志必须 `Print()` (回测可见) | 代码审计 |
| **HC-10** | 诊断面板必须显示 `spread/ATR` 数值 | `BuildDiagnosticReport()` |

### 0.5.3 已被证伪/禁用的设计模式

> 这些在 V8/V9 试过, 实战证明在黄金高点差下不可行, **V10 永久禁用**。

| 反模式 | 失败原因 | V10 替代方案 |
|--------|----------|--------------|
| **马丁倍率加仓** (`Grid_LotMultiplier > 1.0`) | 反复打耳光 → 指数级爆仓 | 等手数网格 + 严格层数 + 硬篮 SL |
| **鞭打过滤器** (60min N 次切换→冻结 4h) | 冻结期吞噬所有交易日 | 新闻日历过滤 (精准时段) |
| **H1 EMA 硬方向过滤** | 与 M15 共振时砍掉所有逆势机会 | L1 软参考 + 路由降级 (非禁止) |
| **趋势冷启动 (等 N 根 K 线)** | 错过状态切换后第一波最有利的行情 | 直接根据当根 K 状态入场 |
| **"中性"状态参与决策** | ADX 反复跨越阈值导致死锁 | 二元状态机 + 防抖 |
| **单 ATR 倍数回调入场** | 强趋势永远不回调到 0.5 ATR 窗口 | 双模式: 回调 + 追单 |
| **保本距离 = 0** (绝对保本) | 黄金高点差下"保本"实际净亏 1×点差 | BE 价 = `open ± spread_price` |
| **微小篮 TP** (USD 15) | 黄金 0.5 USD 波动即破点差成本 | 篮 TP ≥ 点差 × 层数 × 2 |

### 0.5.4 设计哲学声明

```
1. 「不做」比「做」更重要
   - 黄金 70% 时间应该 FREEZE, 而不是找信号
   - 全年 30 笔高质量 > 全年 300 笔平均质量

2. 「点差」是先验, 「ADX/ATR」是后验
   - 任何信号触发前先问: 这笔能赚回 3 倍点差吗?
   - 不能 → 直接丢弃, 不进入状态机决策

3. 「过滤器」是工具, 不是信仰
   - 每加一个过滤器, 必须先证明它能去除真噪声而非真信号
   - V8.2→V8.3 的失败本质: 把所有"可能错"的信号都过滤了

4. 「状态切换」是机会, 不是风险
   - V8.2 冷启动 / V8.3 鞭打过滤的错误在于把切换当风险
   - 真正的风险是「在错误状态下用错误策略」, 不是「切换本身」

5. 「黄金 ≠ 外汇」
   - 不要把 EURUSD 经验套到 XAUUSD
   - 黄金点差是 EURUSD 的 5~20 倍, 策略颗粒度必须粗一个数量级
```

---

## 1. 黄金高点差环境 (必读)

> V10 所有策略设计**必须先过点差护甲**, 再谈 ADX/ATR。  
> 在 Exness Standard / Mini (`XAUUSDm`) 上, 点差 **200~400 点 (2~4 美元/盎司)** 是常态, 不是异常。

### 1.1 点差量化

| 概念 | 公式 / 说明 |
|------|-------------|
| `spread_pts` | `SYMBOL_SPREAD` (点) |
| `spread_price` | `spread_pts × SYMBOL_POINT` (价格距离, 黄金约 2~4 USD) |
| `spread_ATR` | `spread_price / ATR_regime` (点差占 M15 ATR 比例) |
| 开仓即时成本 | BUY 用 Ask, 平仓看 Bid → 天然亏约 1×点差 |
| 保本真实含义 | SL 需补偿 **≥ 1×点差** 才有净保本 |

**示例 (与你实盘一致的数量级)**:

```
品种:     XAUUSDm
点差:     308 点 → ≈ 3.08 USD/盎司
手数:     0.23 手
开仓成本: ≈ 3.08 × 0.23 × 合约乘数 ≈ 数十 USD 量级浮盈前的"隐性亏损"

M15 ATR:  ≈ 3.2~4.5 USD
spread_ATR ≈ 3.08 / 4.0 ≈ 0.77  ← 一单点差吃掉 77% 的 1ATR 波动!
```

**结论**: 在黄金上, **0.5 ATR 的微观策略等于给经纪商打工**; V10 禁止依赖「几美元止盈」的剥头皮逻辑。

### 1.2 V10 点差护甲规则 (全局强制)

以下规则在 `CanOpenNew()` 与子策略入口**统一校验**:

| 规则 ID | 条件 | 动作 | 建议参数 |
|---------|------|------|----------|
| **SP-01** | `spread_pts > InpMaxSpreadPoints` | 拒绝开新仓 | `500` (黄金); 外汇 `50` |
| **SP-02** | `spread_ATR > InpMaxSpreadATRRatio` | 拒绝开新仓 (点差相对波动过大) | `0.35` |
| **SP-03** | 预期盈利目标 `< spread_price × InpMinProfitSpreadMult` | 拒绝该笔信号 | `3.0` (至少赚 3 倍点差才有意义) |
| **SP-04** | 网格整篮 TP (USD) `< spread_cost × 层数 × InpGrid_MinTPSpreadMult` | 禁止开网格首单 | `2.0` |
| **SP-05** | 保本价 | BUY: `open + spread_price`; SELL: `open - spread_price` | 内置, 非可选 |
| **SP-06** | `InpBO_BE_TriggerATR` | ≥ `max(0.8, spread_ATR × 2)` 动态下限 | 避免「显示盈利仍净亏」 |
| **SP-07** | 亚盘 / 周末扩点时段 | 可选时段过滤或提高 SP-01 阈值 | `InpUseSessionFilter` |

### 1.3 策略与点差的兼容性矩阵

| 策略类型 | 黄金高点差适配 | V10 策略 |
|----------|----------------|----------|
| 剥头皮 / M1 高频 | ❌ 极差 | **禁用** |
| 窄幅网格 (间距 < 1 ATR) | ❌ 差 | 仅**窄幅震荡**且间距 ≥ 1.5 ATR |
| 突破 + ATR 宽止损 | ⚠️ 可行 | **强趋势**主策略, SL ≥ 1.5 ATR |
| 趋势跟随 (宽追单窗口) | ⚠️ 可行 | **弱趋势**, 追单上限 ≤ 5 ATR |
| 均值回归 (RSI 极值) | ⚠️ 需大目标 | **低波动+窄幅** 可选模块 |
| 新闻前后冻结 | ✅ 必须 | 继承 V9 日历过滤 |

### 1.4 经纪商与回测建议

| 场景 | 建议 |
|------|------|
| 开发 / 回测 | 在报告中**单独统计点差成本**; 对比「零点差理想」与「300 点现实」 |
| 实盘 | 优先 **Raw/Zero** 账户 (黄金 15~50 点); Standard 仅作保守压力测试 |
| 参数调优 | 任何提高交易频率的参数, 必须先过 `spread_ATR` 检查 |

### 1.5 实战案例: 「浮盈 44 USD 为什么不保本?」(V9.1 复盘)

> 来源: 用户实盘观察 "为什么盈利这么多还不收呢? 只看到这单的止损, 没看到止盈"

**场景重现**:
```
品种:        XAUUSDm
点差:        308 点 = 3.08 USD/盎司
开仓手数:    0.23 手
开仓价:      2657.50 (BUY)
当前 Bid:    2659.40
名义浮盈:    (2659.40 - 2657.50) × 0.23 × 100 = 44 USD ✅

【看起来很美】但策略层视角:
M15 ATR:           ≈ 3.20 USD
当前价格-开仓距离: 1.90 USD = 0.59 × ATR
BE 触发阈值:       1.5 × ATR = 4.80 USD (旧 V8 参数)
                ↑ 远未达到 → 不动 SL ❌

【更糟糕的真相】扣除点差:
真实平仓净价差: 1.90 - 3.08(点差) = -1.18 USD/盎司
真实净浮盈:    -1.18 × 0.23 × 100 = -27 USD (实际是亏的!)
```

**V8 → V9.1 的修复**:

| 参数 | V8 | V9.1 | 含义 |
|------|------|------|------|
| `InpBO_BE_TriggerATR` | 1.5 | 0.8 | 0.8 ATR 即触发保本 |
| `InpBO_TrailStartATR` | 2.5 | 1.5 | 1.5 ATR 起拖移 |
| `InpBO_TrailDistATR` | 0.8 | 0.5 | 拖移距离 0.5 ATR |

**V10 的进一步强化** (SP-06):
```cpp
// V10 计算公式: 黄金高点差时进一步下沉
double dynBE = MathMax(InpBO_BE_TriggerATR, spread_ATR * 2);
// 例: spread_ATR=0.77 → dynBE = max(0.8, 1.54) = 1.54
// 与此同时, BE 设置点位补偿 1×点差:
double bePrice = (isBuy) ? open + spread_price : open - spread_price;
```

**这意味着 V10 的策略层在黄金上**:
- 0.8~1.5 ATR 区间内, **SL 已经移到了 BE+点差**, 即使被打止损也是「净 0」而非「净亏」
- 这是策略层与点差现实**握手言和**的标志

### 1.6 实战案例: 「全年 0 笔交易」(V8.3 / V9.0 复盘)

> 来源: 用户回测后反馈 "好像是一单都没下了"

**根因分解**:
```
V8.3:
  ├─ 鞭打过滤器 (60 min 内 ≥3 次切换 → 冻结 4h) → 全年冻结 ≈ 70%
  ├─ H1 EMA50 vs EMA200 硬过滤 → 屏蔽 60% 信号
  ├─ 趋势冷启动 (等 2 根 K 线) → 错过最快入场
  ├─ 单趋势最多 2 单 → 即使触发也很少
  └─ 综合: 信号率 × 0.3 × 0.4 × 0.7 × 0.5 ≈ 4.2%

V9.0:
  ├─ ADX 中性带 (20~25 区间状态卡死) → 初始 N 根 K 线状态=UNKNOWN
  └─ g_regime 初始 UNKNOWN → 不开仓判断 → 永远不切换

→ 全年 0 ~ 12 笔。
```

**V10 的根本对策** (写入 HC-04 / HC-05 / HC-06):
```
1. 过滤器总数硬上限 3 个 (点差/熔断/新闻 之外不超过 3)
2. ADX 阈值严格二元 (≤22 震荡, ≥22 趋势), 删除「中性带」
3. 初始 g_regime = REGIME_RANGE_NARROW (而非 UNKNOWN)
4. H1 共振默认 SOFT (路由降级而非禁止)
5. 删除「趋势冷启动等待」, 改为「状态切换后第 1 根 K 直接评估」
```

---

## 2. 时间周期体系

### 2.1 全周期列表 (MT5)

| 显示名 | `ENUM_TIMEFRAMES` | 秒 | 宙斯 V10 角色 |
|--------|-------------------|-----|----------------|
| M1 | `PERIOD_M1` | 60 | L4 执行、M1 单 K 振幅盾 |
| M2 | `PERIOD_M2` | 120 | L4 辅助 (可选) |
| M3 | `PERIOD_M3` | 180 | L4 新闻后微观 |
| M4 | `PERIOD_M4` | 240 | L3 过渡 |
| M5 | `PERIOD_M5` | 300 | **L3 信号锚点** (EMA/Donchian) |
| M6 | `PERIOD_M6` | 360 | L3 备选 |
| M10 | `PERIOD_M10` | 600 | L3 日内结构 |
| M12 | `PERIOD_M12` | 720 | L2 备选 |
| M15 | `PERIOD_M15` | 900 | **L2 主状态识别** (默认) |
| M20 | `PERIOD_M20` | 1200 | L2 降噪备选 (黄金推荐 A/B) |
| M30 | `PERIOD_M30` | 1800 | L2 确认层 |
| H1 | `PERIOD_H1` | 3600 | **L1 主趋势** |
| H2 | `PERIOD_H2` | 7200 | L1 共振 |
| H3 | `PERIOD_H3` | 10800 | L1 欧美结构 |
| H4 | `PERIOD_H4` | 14400 | L1 日内趋势 |
| H6 | `PERIOD_H6` | 21600 | L0 波段 |
| H8 | `PERIOD_H8` | 28800 | L0 辅助 |
| H12 | `PERIOD_H12` | 43200 | L0 半日 |
| D1 | `PERIOD_D1` | 86400 | **L0 缺口 / 日线方向** |
| W1 | `PERIOD_W1` | 604800 | L0 周线方向 |
| MN1 | `PERIOD_MN1` | 2592000 | L0 宏观 |

### 2.2 周期分层 (V10 标准架构)

```
┌──────────────────────────────────────────────────────────────────┐
│ L0 宏观    D1 / W1 / MN1     周线方向、重大缺口、日内高低结构        │
├──────────────────────────────────────────────────────────────────┤
│ L1 主趋势  H4 / H1 / H2      EMA 结构、ADX 方向、与 L2 共振过滤     │
├──────────────────────────────────────────────────────────────────┤
│ L2 状态机  M15 / M20 / M30   ADX + ATR + Donch/BB → 12 类市场状态  │
├──────────────────────────────────────────────────────────────────┤
│ L3 信号层  M5 / M10 / M15    Donchian、回调/追单、突破前夜触发       │
├──────────────────────────────────────────────────────────────────┤
│ L4 执行层  M1 + 图表周期      点差护甲、SL/追踪、新闻日历、熔断       │
└──────────────────────────────────────────────────────────────────┘
```

### 2.3 V10 默认周期绑定 (黄金 XAUUSDm)

| 参数 | 默认值 | 层级 |
|------|--------|------|
| `InpRegimeTF` | `PERIOD_M15` | L2 |
| `InpMacroTF` | `PERIOD_H1` | L1 |
| `InpSignalTF` | `PERIOD_M5` | L3 |
| `InpGapTF` | `PERIOD_D1` | L0 |
| `InpExecTF` | 图表周期 (通常 M1) | L4 |

> 黄金噪声大时: 将 `InpRegimeTF` 改为 **M20 或 M30**, 减少状态抖动, 代价是信号滞后。

### 2.4 为什么状态识别用 M15 而不是 M1?

**常见质疑**: "市场状态识别不应该越实时越好吗?"
**回答**: 不一定。`InpRegimeTF` 的选择是 **响应速度 vs 噪声抑制** 的权衡, 黄金这种品种 M1 噪声极大, 实测 M15 是最佳点。

| 周期 | 响应 | 噪声 | 实测问题 |
|------|------|------|----------|
| **M1** | 极快 | 极高 | 黄金 M1 ADX 在 18→25→19→26 反复, 触发 P2 鞭打告警, 状态机抖动 → V8.3 全年 0 笔 |
| **M5** | 较快 | 高 | 仍易被 5 分钟内点差换手 (0.3 ATR) 撞穿 |
| **M15** | 中速 | 适中 | ✅ 当前默认, ADX 趋势识别可信, 防抖根数 2 = 30 分钟确认 |
| **M30** | 慢 | 低 | 滞后 30~60 分钟, 错过 NFP 等快速行情 |

**核心论证**:
1. `InpRegimeMinHoldBars = 2` 意味着状态切换需 2 根 K 收盘确认 — M1 上是 2 分钟, M15 上是 30 分钟。
2. M15 状态切换滞后**对开仓而言不是问题**: V10 的 L3 (M5) 和 L4 (M1) 分别负责信号触发和执行, **状态机只决定路由**, 不亲自下单。
3. 黄金对应单根 M15 K 线波幅约 2~5 美元 (亚盘) / 5~12 美元 (欧美盘), 远大于 M1 的 0.3~1 美元, **信噪比**优势显著。

**何时考虑改 M5**:
- 你做日内极短线 (持仓 ≤ 5 分钟), 信号层 L3 需要更近的状态参考
- 品种点差极低 (≤30 点) 且波动率适中 (主流外汇对)

**何时考虑改 M30**:
- 隔夜挂单 / 多日波段, 不在乎滞后但希望状态稳定
- 黄金极端噪声日 (FOMC 前几小时)

---

## 3. 核心量化指标

在 **`InpRegimeTF`** 上计算 (除非注明)。

| 符号 | 含义 | 计算 |
|------|------|------|
| `ADX` | 趋势强度 | iADX[0] |
| `+DI`, `-DI` | 方向 | iADX[1], [2] |
| `dir` | 多空倾向 | `+DI > -DI` → +1, 否则 -1 |
| `ATR` | 波幅 | iATR |
| `ATR_avg` | 波幅均值 | SMA(ATR, N) |
| `ATR_ratio` | `ATR / ATR_avg` | 波动率状态 |
| `Donch_w` | `(HH-LL)/Close` | N = Donchian 周期 |
| `BB_w` | 布林带宽度比 | 📋 V10 |
| `Gap_atr` | `|O[0]-C[1]|/ATR` | 在 `InpGapTF` |
| `spread_ATR` | 点差 / ATR | **黄金专用** |

---

## 4. 市场状态 (12 类) 与量化阈值

> 默认阈值针对 **XAUUSD / M15**; 其他品种需重新标定。  
> 判定顺序见 **§4.3** (高优先级状态先判)。

### 4.1 状态定义表

| ID | 中文 | 量化条件 (RegimeTF) | 防抖 |
|----|------|---------------------|------|
| **S00** | 高波动 | `ATR_ratio ≥ 2.5` | 2 根 K |
| **S01** | 缺口 | `Gap_atr ≥ 1.5` (D1) | 当日一次 |
| **S02** | 爆发前夜 | `ATR_ratio < 0.8` 持续 ≥5 根 且 当前 `> 1.0` | 2 根 |
| **S03** | 强趋势↑ | `ADX ≥ 35` 且 `dir=+1` | 2 根 |
| **S04** | 强趋势↓ | `ADX ≥ 35` 且 `dir=-1` | 2 根 |
| **S05** | 弱趋势↑ | `22 ≤ ADX < 35` 且 `dir=+1` | 2 根 |
| **S06** | 弱趋势↓ | `22 ≤ ADX < 35` 且 `dir=-1` | 2 根 |
| **S07** | 宽幅震荡 | `ADX < 22` 且 `Donch_w ≥ 0.04` | 2 根 |
| **S08** | 窄幅震荡 | `ADX < 22` 且 `Donch_w ≤ 0.015` | 2 根 |
| **S09** | 盘整 | `ADX < 22` 且 `0.015 < Donch_w < 0.04` | 2 根 |
| **S10** | 低波动 | `ATR_ratio ≤ 0.6` (可与震荡叠加) | 2 根 |
| **S11** | 反转 | 原强趋势后 ADX 跌破 22 + DI 交叉 + 破 L3 EMA | 1 根 (敏感) |
| **S12** | 新闻窗口 | 日历高影响 ±60min | 实时 |

### 4.2 参数名 (V10 `input` 规划)

```text
=== [D] 状态识别 ===
InpRegimeTF           = PERIOD_M15
InpADX_Period         = 14
InpADX_TrendMin       = 22.0      // 趋势下限
InpADX_StrongMin      = 35.0      // 强趋势
InpATR_Period         = 14
InpATR_AvgLookback    = 20
InpATR_HighVolMult    = 2.5
InpATR_LowVolMult     = 0.6
InpRegimeMinHoldBars  = 2
InpRange_NarrowWidth  = 0.015
InpRange_WideWidth    = 0.040
InpSqueeze_Bars       = 5
InpSqueeze_ATR_Low    = 0.8
InpSqueeze_ATR_Break  = 1.0
InpGapTF              = PERIOD_D1
InpGap_ATR_Mult       = 1.5

=== [SP] 点差护甲 (黄金) ===
InpMaxSpreadPoints       = 500
InpMaxSpreadATRRatio     = 0.35
InpMinProfitSpreadMult   = 3.0
InpGrid_MinTPSpreadMult  = 2.0
InpSpreadAwareBE         = true    // 动态抬高 BE 触发
```

### 4.3 判定优先级 (状态机)

```
1.  账户熔断 / 日亏限制?     → 冻结
2.  新闻窗口 S12?             → 冻结开新仓
3.  点差 SP-01 / SP-02?       → 冻结开新仓
4.  缺口 S01?                 → 冻结或仅平仓
5.  高波动 S00?               → 冻结开新仓, 管存量
6.  反转 S11?                 → 平反向单, 暂停跟随
7.  爆发前夜 S02?             → 路由→突破轻仓
8.  强趋势 S03/S04?           → 路由→突破+跟随(全参数)
9.  弱趋势 S05/S06?           → 路由→跟随(缩仓+紧追踪)
10. 低波动 S10?               → 路由→窄幅网格或观望
11. 宽幅震荡 S07?             → 路由→宽网格
12. 窄幅震荡 S08 / 盘整 S09?  → 路由→窄网格或观望
13. 默认                      → 盘整 S09
```

---

## 5. 策略路由表 (核心)

> **路由 = f(市场状态, L1 方向, 点差护甲)**  
> 不满足点差护甲时, 任何状态均 **ROUTE_FREEZE**。

### 5.1 路由枚举 (V10)

| 路由 ID | 名称 | 说明 |
|---------|------|------|
| `ROUTE_FREEZE` | 冻结 | 不开新仓, 仅管理持仓 |
| `ROUTE_GRID_NARROW` | 窄网格 | 小间距, 小 TP, 严格层数上限 |
| `ROUTE_GRID_WIDE` | 宽网格 | 大间距, 大 TP, 适合宽幅震荡 |
| `ROUTE_BREAKOUT` | 突破 | Donchian + 可选 ATR 加速 |
| `ROUTE_TREND_FOLLOW` | 趋势跟随 | 回调 + 追单 (追单上限受点差约束) |
| `ROUTE_BREAKOUT_LIGHT` | 轻仓突破 | 爆发前夜 / 弱确认 |
| `ROUTE_CLOSE_ONLY` | 仅平仓 | 反转、缺口、高波动收尾 |

### 5.2 状态 → 路由 (黄金默认)

| 市场状态 | 路由 | 手数系数 | 出场 profile |
|----------|------|----------|--------------|
| S00 高波动 | `FREEZE` | 0 | — |
| S01 缺口 | `CLOSE_ONLY` | 0 | 快速减仓 |
| S02 爆发前夜 | `BREAKOUT_LIGHT` | 0.5× | 紧 SL, BE@0.8ATR |
| S03 强趋势↑ | `BREAKOUT` + `TREND_FOLLOW` | 1.0× | 激进锁利 A |
| S04 强趋势↓ | 同上 | 1.0× | 激进锁利 A |
| S05 弱趋势↑ | `TREND_FOLLOW` | 0.6× | 紧追踪 B |
| S06 弱趋势↓ | 同上 | 0.6× | 紧追踪 B |
| S07 宽幅震荡 | `GRID_WIDE` | 0.7× | 篮 TP≥点差×层数×2 |
| S08 窄幅震荡 | `GRID_NARROW` 或 `FREEZE` | 0.5× | 高点差时倾向 FREEZE |
| S09 盘整 | `GRID_NARROW` | 0.6× | 介于窄/宽之间 |
| S10 低波动 | `FREEZE` 或 极小网格 | 0.3× | 目标缩小 |
| S11 反转 | `CLOSE_ONLY` | 0 | 平趋势单 |
| S12 新闻 | `FREEZE` | 0 | — |

### 5.3 L1 多周期过滤 (可选)

| `InpMacroMode` | 行为 |
|----------------|------|
| `OFF` | 不看 H1/H4 |
| `SOFT` | 仅诊断日志 (V9 默认) |
| `HARD` | 逆势路由降级: 强趋势↑ 但 H1 空头 → `TREND_FOLLOW` 禁多 |

> 黄金实战建议: 默认 **SOFT**, 避免 V8.3「全年 0 笔」式硬过滤。

### 5.4 出场 Profile (与点差联动)

| Profile | BE 触发 | 追踪启动 | 追踪距离 | 适用 |
|---------|---------|----------|----------|------|
| **A 激进锁利** | `max(0.8, 2×spread_ATR)` ATR | 1.5 ATR | 0.5 ATR | 强趋势 (继承 V9.1) |
| **B 紧跟随** | 0.6 ATR | 1.2 ATR | 0.4 ATR | 弱趋势 |
| **C 网格单** | 无 (整篮 TP) | — | — | 震荡; 单笔 TP ≥ 1 ATR |

### 5.5 路由设计的「反例索引」

> 每条路由的存在都对应一个 V8/V9 的真实失败。理解这个对照表能避免 V11/V12 走回头路。

| 路由 | 「反例」即 V8/V9 的对应失败 |
|------|------------------------------|
| `ROUTE_FREEZE` 对 S00/S01/S12 | V8.0 高点差时仍尝试开仓 → 立即被点差吃 + ATR 突变止损 → 灾难 |
| `ROUTE_GRID_NARROW` 黄金默认关 | V8.1 反复打耳光行情 + 1.6× 马丁 → -97% (350→267 USD) |
| `ROUTE_BREAKOUT_LIGHT` 爆发前夜半仓 | V8.0 突破后第一波满仓 → 被假突破打止损 → 真趋势启动时已减仓 |
| `ROUTE_TREND_FOLLOW` 含追单模式 | V9.0 仅回调 → 强趋势永远不回调到 0.5 ATR → 全程错过 |
| `ROUTE_CLOSE_ONLY` 反转/缺口 | V8 缺口跳空状态混乱 → 用震荡参数处理趋势 → 单单挂止损 |
| 出场 Profile A 激进锁利 | V9.0 浮盈 44 USD 不动 SL → 回吐后才止损 (用户实盘投诉) |
| 出场 Profile C 整篮 TP | V8.1 单笔小 TP (USD 15) < 点差成本 → 永远不可能达到 |

---

## 6. 子策略模块说明

### 6.1 突破引擎 (`ROUTE_BREAKOUT`)

- **周期**: L3 `M5` Donchian; L2 状态为强趋势
- **入场**: `Ask > HH` / `Bid < LL`; 可选 `ATR_ratio` 加速
- **止损**: `1.5 × ATR` (≥ 点差 × 2)
- **止盈**: 无固定 TP; Profile A 追踪
- **点差**: 必须通过 SP-01~03

### 6.2 趋势跟随 (`ROUTE_TREND_FOLLOW`)

- **锚点**: L3 EMA(`InpSignalTF`, 20)
- **回调**: `|price - anchor| ≤ 1.5 × ATR`
- **追单**: `1.5 < dist ≤ 5.0 × ATR` (黄金必需, 否则永远不进场)
- **点差**: 追单上限随 `spread_ATR` 动态收缩: `MaxChase = min(5.0, 3.0/spread_ATR)`

### 6.3 网格引擎 (`ROUTE_GRID_NARROW / WIDE`)

| 类型 | 间距 | 最大层数 | 篮 TP (USD) | 点差约束 |
|------|------|----------|-------------|----------|
| 窄 | `0.8~1.2 ATR` | 3 | ≥ 25 | 高点差时 **默认关闭** |
| 宽 | `1.5~2.0 ATR` | 4 | ≥ 40 | 层数×点差成本 < 篮 TP/2 |

- 状态切趋势时: **强平网格** (继承 `InpGrid_CloseOnTrendSwitch`)
- V10 默认黄金: `InpEnableGrid = false`, 仅在 S07/S08/S09 且 `spread_ATR < 0.25` 时自动开启

### 6.4 新闻日历 (全局)

继承 V9 `CalendarValueHistory`:

- 高影响新闻前后 `±60` 分钟 → `ROUTE_FREEZE`
- 与状态机独立, **优先级最高**

---

## 7. 软件架构 (V10 代码结构规划)

```
Zeus_V10.mq5
├── OnInit / OnDeinit / OnTick
├── Layer0_Macro.cpp      // D1/W1 缺口、方向
├── Layer1_TrendBias.cpp  // H1/H4 EMA+ADX
├── Layer2_Regime.cpp     // 12 类状态机
├── Layer3_Signals.cpp    // Donchian / TrendFollow
├── SpreadGuard.cpp       // SP-01 ~ SP-07
├── Router.cpp            // 状态 → ROUTE_*
├── Engine_Breakout.cpp
├── Engine_Grid.cpp
├── Engine_Exit.cpp       // Profile A/B/C
├── Risk.cpp              // 熔断、日亏、手数
├── NewsFilter.cpp
└── Diagnostics.cpp       // 宙斯 V10 诊断面板
```

**关键函数**:

```cpp
bool PassesSpreadGuard(string &reason);           // 全局
MarketState ClassifyRegime();                     // L2
StrategyRoute ResolveRoute(MarketState s);        // 路由表
void ExecuteRoute(StrategyRoute r);                 // 分发引擎
```

---

## 8. 诊断输出规范 (宙斯 V10)

```
──────── 宙斯 V10 诊断 ────────
【点差】308 点 (3.08 USD) spread/ATR=0.77 ⚠ 偏高 | 上限 500
【状态】弱趋势↓ (S06) | ADX=28.1 ATR=4.12 ratio=1.05 Donch=2.1%
【路由】TREND_FOLLOW | 手数系数 0.6 | 出场 Profile B
【L1】H1 多头 [软参考] | H4 ADX=31
【信号】空头追单? 是 dist=2.8ATR (上限 4.2)
【新闻】✓ 正常 | 下次 NFP 还有 180 分钟
【风控】可开新仓=是
────────────────────────
```

---

## 9. V10 实施路线图 (问题驱动)

> 每个阶段必须绑定一个**已知 V8/V9 失败**和明确**验收指标**, 否则不开工。

| 阶段 | 解决的旧问题 | 内容 | 验收指标 |
|------|-------------|------|----------|
| **P0** 重塑 | V8.0 品种感知缺失 | 从 `V9.mq5` 复制为 `Zeus_V10.mq5`, 品牌切换, OnInit 根据 `_Symbol` 自动 set | 黄金启动日志: `MaxSpread=500 (XAU auto)` |
| **P1** 点差护甲 | V8.1 灾难 / V9.1 44USD 案例 | `SpreadGuard.cpp` 实现 SP-01~07, BE 计算包含 `spread_price` | 黄金回测: 入场前必经过 7 条检查, BE 价 ≠ 开仓价 |
| **P2** 二元状态机 | V9.0 中性带死锁 | 删除 `REGIME_NEUTRAL`, 默认 `RANGE_NARROW`, ADX 二元化, 防抖根数=2 | 启动后 ≤5 根 K 必产出第一个有效状态 |
| **P3** 12 类状态扩展 | V8 状态颗粒太粗 (5 类用同一参数) | 拆分强/弱趋势、宽/窄震荡, 各自专属参数集 | 12 个状态在样本日志中都至少出现 1 次 |
| **P4** 路由表 | V8/V9 if-else 引擎分支难维护 | `Router.cpp` 用 `state→ROUTE_` 映射替代 if-else | 路由变更只改 1 张表, 引擎代码不动 |
| **P5** 缺口/反转/前夜 | V8 缺口跳空策略错配 | 加入 S01/S02/S11 处理 | 周一开盘缺口日志: `S01 gap → CLOSE_ONLY` |
| **P6** 出场 Profile A/B/C | V8.2.1 R:R 0.70 / V9.1 44USD | 按路由派发不同 BE/Trail | 黄金回测 R:R ≥ 1.2 |
| **P7** 防回归测试 | 每次新版本意外破坏老功能 | 建立 4 个回归案例: 高点差/全年0笔/中性带/44USD | 任何 PR 必须 4/4 通过 |
| **P8** 黄金优化包 | — | `Zeus_XAUUSD.set` + 回测报告模板 + 实盘部署清单 | 一年回测 PF≥1.3, MaxDD ≤ 15% |

### 9.1 验收标准的「反向定义」

V10 **必须**通过以下回归测试 (对应历史失败):

| 测试 ID | 场景 | 通过条件 |
|---------|------|----------|
| **REG-01** | 加载到 XAUUSDm M1 默认参数 | 启动 30 天内有 ≥10 笔交易 (否定 V8.3/V9.0 全年 0 笔) |
| **REG-02** | 回测 2025-01~2025-12 | 最大回撤 ≤ 15% (否定 V8.1 -97%) |
| **REG-03** | ADX 在 22 附近横盘 200 根 K | 状态切换次数 ≤ 5 次 (否定 V9.0 中性带抖动) |
| **REG-04** | 突破单浮盈达 0.8 ATR | SL 已移至 `open + spread_price` (否定 V9.1 44USD) |
| **REG-05** | NFP 前后 60 分钟 | 路由必定 = `FREEZE` (继承 V9 新闻能力) |
| **REG-06** | 周一开盘缺口 > 0.5% | 路由 = `CLOSE_ONLY` 至少 1 根 K (V10 新增) |

---

## 10. 版本对照

| 能力 | 夔牛 V9.1 | 宙斯 V10 |
|------|-----------|----------|
| 品牌 / 文件 | 夔牛 `V9.mq5` | 宙斯 `Zeus_V10.mq5` |
| 设计中心 | 双引擎 + 过滤器堆叠 | **周期 + 状态 → 路由表** |
| 市场状态 | 5 类 | **12 类** |
| 点差护甲 | 仅 MaxSpread | **7 条规则 + spread/ATR** |
| 网格默认 | 关 | 高点差自动关, 震荡有条件开 |
| 出场 | 单一 Profile | **A/B/C 按路由** |
| H1 | 软参考 | 软/硬可配置 |
| 状态初始化 | `UNKNOWN` (会死锁) | `RANGE_NARROW` |
| ADX 阈值 | 中性带 20-25 | 二元 = 22 |
| BE 计算 | 单纯 ATR 倍数 | `max(ATR×k, 2×spread_ATR)` + 点差补偿 |
| 实战教训内化 | 隐含 | **明文 §0.5 + §1.5/1.6 + §5.5 + §9.1** |

---

## 11. 参考资料

- 历史复盘: [V8_V9_版本演进说明.md](V8_V9_版本演进说明.md)
- 代码基线: `V9.mq5`
- 借鉴: `GMarket.mq5` (新闻), `复刻牛.mq5` (M1 盾), `天天向上` (马丁 — **V10 不默认采用**)

---

## 12. 开发前决策 (已定稿 2026-05-22)

> 以下决策为开工前定稿; 若需变更, 必须在 §13 变更日志登记并更新对应 §0.5 教训映射。

| # | 决策 | 实施载体 | V10 阶段 |
|---|------|----------|---------|
| **Q1** | ✅ **切 Raw/Zero 账户** (黄金点差 15~50 点) | OnInit 检测点差, 若 >100 给出强提示 | P0 配套文档 |
| **Q2** | ✅ **亚盘 23:00~04:00 GMT 仅放宽 SP-01 1.5 倍**, SP-02 不变 | `IsAsianSession() → spreadCap × 1.5` | P1 |
| **Q3** | ✅ **分阶段实现 12 类状态** | P3 做 6 类 (S00/S03/S04/S07/S09/S12), P4 补 6 类 | P3 / P4 |
| **Q4** | ✅ **网格降级为机会单** | 默认关 + 三重激活条件 (S07/S08 + spread_ATR<0.25 + H1_ADX<20) | P3 |
| **Q5** | ⚠️ **V10 引入轻量 ML** (用户决策, 与默认倾向相反) | 见 §12.1 ML 模块设计约束 | P4 后追加 P4.5 |
| **Q6** | ⚠️ **追单上限即做自适应** (用户决策, 与默认倾向相反) | 见 §12.2 自适应追单上限设计 | P3 |
| **Q7** | ✅ **V10 锁死单账户单图表** | 任何多账户特性写入 V11+ 路线图 | 永久 |

### 12.1 ML 模块设计约束 (Q5 实施)

> 用户决策引入 ML, 但必须避免 "把 ML 当万能药" 重蹈过滤器堆叠覆辙。以下是 V10 ML 的**红线**:

| 约束 ID | 内容 | 理由 |
|---------|------|------|
| **ML-01** | ML **仅用于状态分类辅助**, 不直接产出交易信号 | 防止黑箱交易; ML 错了规则引擎能兜底 |
| **ML-02** | ML 输出必须落到现有 12 类 `MarketState` 枚举 | 不引入新状态空间, 接口稳定 |
| **ML-03** | ML **置信度 < 0.6** 时退回纯 ADX/ATR 规则分类器 | 不确定时用确定性逻辑 |
| **ML-04** | 训练数据**最少 6 个月 M15 K 线**, 否则禁用 | 黄金状态切换稀疏, 样本不足会过拟合 |
| **ML-05** | 模型推理走 **MT5 内嵌 ONNX**, 不引入外部依赖 | 部署链路最短 |
| **ML-06** | ML 决策每 N 笔交易后做 **A/B 对比** (与纯规则), 劣于规则则自动 disable | 防止线上失效 |
| **ML-07** | 特征工程**只用价格/成交量/ATR/ADX 衍生量**, 禁用未来函数 | 数据泄露防护 |
| **ML-08** | ML 训练脚本独立于 EA 主代码, 走 Python 离线训练 → ONNX 导出 | 关注点分离 |

**实施步骤** (P4.5 阶段):
1. Python 离线: 用历史 M15 数据 + 人工标注或半监督生成 12 类状态标签
2. 训练轻量模型 (XGBoost / 浅层 MLP)
3. 导出 ONNX → 放入 `MQL5/Files/zeus_state.onnx`
4. EA 启动加载, 推理结果与 `ClassifyRegime()` 双源对比
5. 灰度上线: 先 SHADOW 模式 (只记录不决策) 跑 1 个月, 再切 ACTIVE

**默认参数**:
```mql5
input bool InpEnableMLClassifier = false;  // 默认关, 训练完毕再开
input string InpMLModelPath = "zeus_state.onnx";
input double InpMLMinConfidence = 0.6;
input ENUM_ML_MODE InpMLMode = ML_SHADOW;  // SHADOW / ACTIVE
```

### 12.2 自适应追单上限设计 (Q6 实施)

> 用户决策即做, 写法必须避开 V8 "过度优化" 陷阱: 自适应不等于黑箱。

| 设计要素 | V10 规格 |
|----------|----------|
| 输入 | 近 `N=20` 周的 M15 ATR 序列 |
| 计算 | `MaxChase_ATR = percentile(ATR_history, 80) / current_ATR × InpBO_TF_BaseChase` |
| 兜底 | 若 `N<20` 周数据不足 → 退回 `InpBO_TF_BaseChase = 5.0` |
| 边界 | `clamp(MaxChase_ATR, 2.0, 8.0)` 防止极端值 |
| 刷新 | 每周一开盘第一根 K 时重算, 期间冻结 |
| 日志 | 每次刷新必 `Print` 新旧值 (HC-09) |
| 诊断面板 | 必显示当前 `MaxChase=X.X ATR (auto)` (HC-10) |

**默认参数**:
```mql5
input bool InpAdaptiveMaxChase = true;       // 启用自适应
input int InpAdaptiveLookbackWeeks = 20;     // 回看周数
input double InpBO_TF_BaseChase = 5.0;       // 兜底/基线
input double InpMaxChase_Lower = 2.0;        // 自适应下限
input double InpMaxChase_Upper = 8.0;        // 自适应上限
```

**与 §0.5 教训的对应关系**:
- ✅ 不违反 HC-04 (这不是入场过滤器, 是入场窗口宽度)
- ✅ 满足 HC-09 (日志可追溯)
- ✅ 满足 HC-10 (诊断面板可见)
- ⚠️ **需要新加 REG-07 回归测试**: 数据不足时必须退回 5.0, 不能崩

### 12.3 因 Q5/Q6 调整的路线图

由于引入 ML 与自适应追单, 原 §9 路线图新增/调整:

| 阶段 | 原状态 | 调整 |
|------|--------|------|
| P3 | 12 类状态前 6 类 | + 自适应追单 `EstimateMaxChase()` |
| **GATE-A** (P4 后) | — | **ML 必要性再评估检查点** (见 §12.4) |
| **P4.5** (条件启动) | — | ML 离线训练 + ONNX 导出 + SHADOW 模式上线 |
| P5 | 黄金 `Zeus_XAUUSD.set` | + ML 模型文件交付 (若 P4.5 启动) |

并新增回归测试:

| ID | 场景 | 通过条件 |
|----|------|----------|
| **REG-07** | 历史数据 < 20 周 | `MaxChase` 必须 = 5.0, 无报错 |
| **REG-08** | ONNX 模型缺失 | 必须自动退回规则引擎, 不崩溃 |
| **REG-09** | ML 置信度 < 0.6 | 状态必须来自 `ClassifyRegime()` 而非 ML |

### 12.4 GATE-A: ML 必要性再评估检查点

> **触发时机**: P0~P4 全部完成且通过 REG-01~06 后, 启动 P4.5 之前。  
> **决策依据**: 用纯规则引擎跑出来的回测结果说话, 不是凭直觉。

**通过 GATE-A 进入 P4.5 (启动 ML) 的条件**:

需要**同时满足**以下 ≥3 项才启动 P4.5:

| 指标 | 阈值 | 含义 |
|------|------|------|
| 1. 规则引擎 PF (Profit Factor) | < 1.5 | 还有优化空间 |
| 2. 规则引擎 MaxDD | > 12% | 状态识别还不够准 |
| 3. 状态误判率 (人工抽样 100 例) | > 25% | 规则边界确实有死角 |
| 4. 同一参数集在不同市场期 (上半年/下半年) | PF 差异 > 50% | 规则不够鲁棒, ML 可能帮上忙 |
| 5. 趋势/震荡切换点附近 50 根 K 内胜率 | < 30% | 切换识别滞后, ML 可改善 |

**触发跳过 P4.5 (取消 ML, 直接进 P5) 的条件**:

任一满足即跳过:

| 指标 | 阈值 | 含义 |
|------|------|------|
| A. 规则引擎 PF | ≥ 1.5 | 规则已够好, ML 边际收益低 |
| B. 规则引擎 MaxDD | ≤ 10% | 风险已可控 |
| C. 全年交易数 | 80~200 笔范围内 | 频次合理, 不需要更激进识别 |

**决策表**:

```
P4 完成后:
├─ 收集 1 年回测指标
├─ 抽样 100 次状态切换人工标注
├─ 对照上表评分
└─ 给用户输出 GATE-A 报告 → 用户最终拍板是否启动 P4.5

GATE-A 报告示例:
   "规则引擎 PF=1.62, MaxDD=8.3%, 138 笔, 状态误判率 12%
    建议: 跳过 P4.5 ML, 节省 2 周开发时间, 直接进入 P5 实盘部署"
   或
   "规则引擎 PF=1.21, MaxDD=14.5%, 53 笔, 状态误判率 31%
    建议: 启动 P4.5 ML, 重点改善震荡→趋势切换识别"
```

**这个检查点的价值**: 把「凭感觉决定要不要 ML」改成「凭实测数据决定」, 直接对应 §0.5 第 5 条设计哲学:  
> "「过滤器」是工具, 不是信仰" — ML 同理, 用得上再加, 用不上不加。

---

## 13. 文档变更日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-05-22 | 0.1 | 初始 V10 设计稿 (周期 + 状态 + 路由 + 点差护甲) |
| 2026-05-22 | 0.2 | **实战驱动优化**: 新增 §0.5 (10 教训 + 10 硬约束 + 8 禁用反模式) / §1.5 44USD 案例 / §1.6 全年 0 笔案例 / §5.5 反例索引 / §9.1 6 个回归测试 / §10 扩展对照 / §12 开放问题 |
| 2026-05-22 | 0.3 | **§12 决策定稿**: Q1 切 Raw / Q2 亚盘点差放宽 1.5× / Q3 分阶段 12 状态 / Q4 网格降级 / **Q5 引入 ML (用户决策, 含 ML-01~08 红线)** / **Q6 自适应追单 (用户决策, 含设计规格)** / Q7 锁单账户; 新增 P4.5 阶段 + REG-07/08/09 |
| 2026-05-22 | 0.4 | **新增 GATE-A 检查点 (§12.4)**: P4 完成后基于实测数据决定是否启动 P4.5 ML, 防止过度工程化 |
| 2026-05-23 | 0.5 | **P0/P1 编码完成并实盘启动验证通过**: Zeus_V10.mq5 已就位 (1877 行), 启动日志正确显示品种感知/SpreadGuard/spread_ATR=0.03; **UI 面板 V10 重设计**: 折叠/展开按钮, 移除手动买卖 (§0.5 反模式), 新增日盈利显示与达标自动暂停 (参数组 [C2], 默认 2%), 跨日自动恢复 |
| 2026-05-23 | 0.6 | **P2 二元状态机加固完成**: 日盈利标签移至左侧 dashboard; **HC-09 状态切换证据链** (4 行多行日志含量化证据/防抖根数/ATR-ADX/距上次切换时长/1h-24h 切换频率与鞭打告警); **REG-01 自检** (启动 ≤5 根 K 须产出首个状态, ≥5 分钟未产出主动告警); **状态切换历史环形缓冲区 32 槽** + `RecordRegimeSwitch_Zeus / CountRegimeSwitches_Zeus`; **诊断面板新增「状态稳定」行** (上次切换时间 + 1h/24h 次数 + 健康度判断); 启动横幅显示 P2 状态机参数 |
| 2026-05-23 | 0.7 | **P3 多状态 + 路由表完成 (前 6 类: S00/S03/S04/S07/S09/S12)**: MarketRegime 枚举扩展 `REGIME_RANGE_WIDE` + `REGIME_NEWS_WINDOW`; 新增 `StrategyRoute` 枚举 (FREEZE/GRID_NARROW/GRID_WIDE/BREAKOUT/CLOSE_ONLY); `GetDonchianWidthPct()` 计算通道宽度区分 S07/S09; `ResolveRoute_Zeus()` 实现 §5.2 路由表 + §5.5 反例索引 (每分支含失败案例追溯); `ManageGridEngine` 兼容 S07 (宽网格间距 ×3); 诊断面板新增「路由」行 + 左侧 dashboard 显示路由 + Donch_w; 启动横幅显示 P3 路由表参数; 5 个新输入参数 (`InpEnableP3States/InpDonch_Period/InpDonchWideThresholdPct/InpDonchNarrowFloorPct/InpADX_StrongTrend`) |
| 2026-05-23 | 0.8 | **P4 全 12 类状态识别完成 (新增 S01/S02/S05/S06/S08/S10/S11)**: MarketRegime 扩展 `REGIME_GAP/PRE_BREAKOUT/TREND_UP_WK/TREND_DN_WK/RANGE_NARROW/LOW_VOL/REVERSAL` 七个值; StrategyRoute 新增 `ROUTE_BREAKOUT_LIGHT (S02 轻仓)` + `ROUTE_TREND_FOLLOW (S05/S06 紧追踪)`; 三个辅助函数: `GetGapATR_Zeus` 检测 S01 缺口 / `IsPreBreakout_Zeus` 检测 S02 蓄势 / `IsReversal_Zeus` 检测 S11 反转 (前强趋势+ADX跌破+DI翻转); `ClassifyRegime_Zeus` 优先级链 12 级: 新闻>高波动>缺口>反转>前夜>强趋势>弱趋势>低波动>宽幅>窄幅>盘整(兜底); 路由表覆盖全 12 状态 (S01/S11 → CLOSE_ONLY, S02 → BREAKOUT_LIGHT, S05/S06 → TREND_FOLLOW, S08 高点差→FREEZE/低点差→GRID_NARROW, S10 → FREEZE); 7 个新输入参数 (`InpEnableP4States/InpGap_ATR_Mult/InpPreBreakout_LowRatio/InpPreBreakout_LowBars/InpPreBreakout_NowRatio/InpLowVol_ATRRatio/InpReversal_DI_Cross`); **UI 修复**: dashboard 加深色半透明背景框 (`OBJ_RECTANGLE_LABEL` + ZORDER 控制) 解决 V10 P3 期间左侧面板在亮色 K 线下不可见的问题, OnInit 末尾立即首次刷新 dashboard, 新增 2 个 UI 输入参数 (`InpDashboardBackground/InpDashboardFontSize` 默认 10) |

---

*本文档为宙斯 EA V10 的权威设计规格; 实现以 `Zeus_V10.mq5` 中 `input` 默认值为准。黄金交易务必在回测中开启真实点差模型。*  
*所有设计决策必须能追溯到 §0.5 的某条教训或某个 V8/V9 真实事件; 任何"想加但找不到根据"的特性, 写入 §12 待回答问题。*
